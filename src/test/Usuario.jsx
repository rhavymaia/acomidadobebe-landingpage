import { Link } from "react-router-dom";

export const Usuario = () => {
    return (
        <div>
            <h1>Usuário</h1>
            <Link to="/">retornar a página inicial</Link>
        </div>
    );
}
